/* eslint-disable prettier/prettier */
import React from 'react';
import { View, Text } from 'react-native';

// import { Container } from './styles';

export default function Notifictions({route}) {
    return(
        <View>
            <Text>Notifictions</Text>
        </View>
    );
}